prompts:
  command_injection:
    base: 'SECURITY FINDING: Potential command injection via Process.Start with string concatenation.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this command injection risk. Do NOT review the rest of the file for other issues.

      1. Check whether user-controlled input flows into the process arguments.

      2. If vulnerable, propose using an argument array (no shell), input validation, or allowlisting.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: Potential command injection via Process.Start with string concatenation.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this command injection risk. Do NOT review the rest of the file for other issues.

      1. Check whether user-controlled input flows into the process arguments.

      2. If vulnerable, propose using an argument array (no shell), input validation, or allowlisting.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: Potential command injection via Runtime.exec() or ProcessBuilder with user input.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this command injection risk. Do NOT review the rest of the file for other issues.

      1. Check whether user-controlled input flows into the process arguments.

      2. If vulnerable, propose using ProcessBuilder with array arguments, input validation, or allowlisting.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  deep_inheritance:
    base: 'CODE QUALITY: Class implements many interfaces or has complex inheritance.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this inheritance hierarchy. Do NOT review the rest of the file for other issues.

      1. Determine if the interfaces indicate too many responsibilities (violating Interface Segregation).

      2. Consider composition over inheritance or splitting the class.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'CODE QUALITY: Class implements many interfaces or has complex inheritance.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this inheritance hierarchy. Do NOT review the rest of the file for other issues.

      1. Determine if the interfaces indicate too many responsibilities (violating Interface Segregation).

      2. Consider composition over inheritance or splitting the class.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'CODE QUALITY: Class implements many interfaces or has complex inheritance.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this inheritance hierarchy. Do NOT review the rest of the file for other issues.

      1. Determine if the interfaces indicate too many responsibilities (violating Interface Segregation).

      2. Consider composition over inheritance or splitting the class.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  deep_nesting:
    base: 'CODE QUALITY: Excessive nesting depth (>4 levels).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deeply nested code. Do NOT review the rest of the file for other issues.

      1. Identify guard clauses, early returns, or method extraction to reduce nesting.

      2. Propose a restructured version using inversion of control flow.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'CODE QUALITY: Excessive nesting depth (>4 levels).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deeply nested code. Do NOT review the rest of the file for other issues.

      1. Identify guard clauses, early returns, or method extraction to reduce nesting.

      2. Propose a restructured version using inversion of control flow.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'CODE QUALITY: Excessive nesting depth (>4 levels).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deeply nested code. Do NOT review the rest of the file for other issues.

      1. Identify guard clauses, early returns, or method extraction to reduce nesting.

      2. Propose a restructured version using inversion of control flow.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  exception_swallowing:
    base: 'BUG: Empty catch block swallows exception silently.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this empty catch block. Do NOT review the rest of the file for other issues.

      1. Determine what the catch block should do: log, rethrow, wrap, or handle the exception.

      2. Based on the surrounding context, propose the appropriate handler (ILogger, rethrow with throw;, etc.).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'BUG: Empty catch block swallows exception silently.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this empty catch block. Do NOT review the rest of the file for other issues.

      1. Determine what the catch block should do: log, rethrow, wrap, or handle the exception.

      2. Based on the surrounding context, propose the appropriate handler (ILogger, rethrow with throw;, etc.).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'BUG: Empty catch block swallows exception silently.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this empty catch block. Do NOT review the rest of the file for other issues.

      1. Determine what the catch block should do: log, rethrow, wrap, or handle the exception.

      2. Based on the surrounding context, propose the appropriate handler (Logger, rethrow with throw, etc.).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  god_method:
    base: 'CODE QUALITY: Method exceeds 100 lines.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this oversized method. Do NOT review the rest of the file for other issues.

      1. Identify logical blocks within the method that can be extracted into smaller methods.

      2. Propose an extract-method refactoring that preserves behavior.

      3. Show the method decomposition plan -- do not refactor other methods in the file.'
    csharp: 'CODE QUALITY: Method exceeds 100 lines.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this oversized method. Do NOT review the rest of the file for other issues.

      1. Identify logical blocks within the method that can be extracted into smaller methods.

      2. Propose an extract-method refactoring that preserves behavior.

      3. Show the method decomposition plan -- do not refactor other methods in the file.'
    java: 'CODE QUALITY: Method exceeds 100 lines.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this oversized method. Do NOT review the rest of the file for other issues.

      1. Identify logical blocks within the method that can be extracted into smaller methods.

      2. Propose an extract-method refactoring that preserves behavior.

      3. Show the method decomposition plan -- do not refactor other methods in the file.'
  hardcoded_secret:
    base: "SECURITY FINDING: Hardcoded secret detected.\nFile: {file} (line {line})\nContext: {context}\n\nTASK: Evaluate\
      \ ONLY this hardcoded secret. Do NOT review the rest of the file for other issues.\n1. Confirm whether the value is\
      \ a real secret (password, API key, connection string) or a false positive.\n   Common false positives: key-name constants\
      \ used for parsing (e.g. const string PASSWORD = \"password\"),\n   connection string builders/parsers, secret removal/masking/redaction\
      \ methods, logging placeholders.\n2. If it is a real secret, propose a fix: move to environment variable, Azure Key\
      \ Vault, user-secrets, or IConfiguration injection.\n3. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
    csharp: "SECURITY FINDING: Hardcoded secret detected.\nFile: {file} (line {line})\nContext: {context}\n\nTASK: Evaluate\
      \ ONLY this hardcoded secret. Do NOT review the rest of the file for other issues.\n1. Confirm whether the value is\
      \ a real secret (password, API key, connection string) or a false positive.\n   Common false positives: key-name constants\
      \ used for parsing (e.g. const string PASSWORD = \"password\"),\n   connection string builders/parsers, secret removal/masking/redaction\
      \ methods, logging placeholders.\n2. If it is a real secret, propose a fix: move to environment variable, Azure Key\
      \ Vault, user-secrets, or IConfiguration injection.\n3. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
    java: "SECURITY FINDING: Hardcoded secret detected.\nFile: {file} (line {line})\nContext: {context}\n\nTASK: Evaluate\
      \ ONLY this hardcoded secret. Do NOT review the rest of the file for other issues.\n1. Confirm whether the value is\
      \ a real secret (password, API key, connection string) or a false positive.\n   Common false positives: key-name constants\
      \ used for parsing (e.g. const string PASSWORD = \"password\"),\n   connection string builders/parsers, secret removal/masking/redaction\
      \ methods, logging placeholders.\n2. If it is a real secret, propose a fix: move to environment variable, Azure Key\
      \ Vault, user-secrets, or IConfiguration injection.\n3. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
  insecure_deserialization:
    base: 'SECURITY FINDING: Insecure deserialization detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deserialization issue. Do NOT review the rest of the file for other issues.

      1. Identify the dangerous type (BinaryFormatter, SoapFormatter, TypeNameHandling.All, etc.).

      2. Propose a safe replacement: System.Text.Json, JsonSerializer with safe settings, or a custom binder.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: Insecure deserialization detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deserialization issue. Do NOT review the rest of the file for other issues.

      1. Identify the dangerous type (BinaryFormatter, SoapFormatter, TypeNameHandling.All, etc.).

      2. Propose a safe replacement: System.Text.Json, JsonSerializer with safe settings, or a custom binder.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: Insecure deserialization detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this deserialization issue. Do NOT review the rest of the file for other issues.

      1. Identify the dangerous type (ObjectInputStream.readObject() without validation, etc.).

      2. Propose a safe replacement: Jackson with safe settings or custom deserializer.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  insecure_random:
    base: 'SECURITY FINDING: System.Random used in a security-sensitive context.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this random number usage. Do NOT review the rest of the file for other issues.

      1. Confirm whether the value is used for tokens, passwords, nonces, or other security purposes.

      2. If so, replace System.Random with RandomNumberGenerator (System.Security.Cryptography).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: System.Random used in a security-sensitive context.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this random number usage. Do NOT review the rest of the file for other issues.

      1. Confirm whether the value is used for tokens, passwords, nonces, or other security purposes.

      2. If so, replace System.Random with RandomNumberGenerator (System.Security.Cryptography).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: java.util.Random used in a security-sensitive context.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this random number usage. Do NOT review the rest of the file for other issues.

      1. Confirm whether the value is used for tokens, passwords, nonces, or other security purposes.

      2. If so, replace java.util.Random with SecureRandom (java.security.SecureRandom).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  long_parameter_list:
    base: 'CODE QUALITY: Method has more than 5 parameters.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this parameter list. Do NOT review the rest of the file for other issues.

      1. Group related parameters into a parameter object, record, or Options class.

      2. Consider whether Builder pattern or method overloads would be more appropriate.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'CODE QUALITY: Method has more than 5 parameters.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this parameter list. Do NOT review the rest of the file for other issues.

      1. Group related parameters into a parameter object, record, or Options class.

      2. Consider whether Builder pattern or method overloads would be more appropriate.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'CODE QUALITY: Method has more than 5 parameters.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this parameter list. Do NOT review the rest of the file for other issues.

      1. Group related parameters into a parameter object, record, or Options class.

      2. Consider whether Builder pattern or method overloads would be more appropriate.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  magic_number:
    base: 'STYLE: Hardcoded numeric literal in logic.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this magic number. Do NOT review the rest of the file for other issues.

      1. Determine the meaning of the number from context.

      2. Extract it to a named constant with a descriptive name.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'STYLE: Hardcoded numeric literal in logic.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this magic number. Do NOT review the rest of the file for other issues.

      1. Determine the meaning of the number from context.

      2. Extract it to a named constant with a descriptive name.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'STYLE: Hardcoded numeric literal in logic.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this magic number. Do NOT review the rest of the file for other issues.

      1. Determine the meaning of the number from context.

      2. Extract it to a named constant with a descriptive name.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  missing_null_check:
    base: 'STYLE: Public method parameter lacks null check.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this null-check gap. Do NOT review the rest of the file for other issues.

      1. Determine if the parameter is nullable and needs validation.

      2. Add ArgumentNullException.ThrowIfNull() or a null-check guard clause.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'STYLE: Public method parameter lacks null check.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this null-check gap. Do NOT review the rest of the file for other issues.

      1. Determine if the parameter is nullable and needs validation.

      2. Add ArgumentNullException.ThrowIfNull() or a null-check guard clause.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'STYLE: Public method parameter lacks null check.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this null-check gap. Do NOT review the rest of the file for other issues.

      1. Determine if the parameter is nullable and needs validation.

      2. Add ArgumentNullException.ThrowIfNull() or a null-check guard clause.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  mutable_shared_state:
    base: 'STYLE: Static mutable field (potential thread-safety issue).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this static mutable field. Do NOT review the rest of the file for other issues.

      1. Determine if the field is accessed from multiple threads.

      2. If so, make it readonly, use ConcurrentDictionary, Lazy<T>, or add synchronization.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'STYLE: Static mutable field (potential thread-safety issue).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this static mutable field. Do NOT review the rest of the file for other issues.

      1. Determine if the field is accessed from multiple threads.

      2. If so, make it readonly, use ConcurrentDictionary, Lazy<T>, or add synchronization.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'STYLE: Static mutable field (potential thread-safety issue).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this static mutable field. Do NOT review the rest of the file for other issues.

      1. Determine if the field is accessed from multiple threads.

      2. If so, make it final, use ConcurrentDictionary, Lazy<T>, or add synchronization.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  open_redirect:
    base: 'SECURITY FINDING: Potential open redirect.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this redirect. Do NOT review the rest of the file for other issues.

      1. Confirm whether user input (returnUrl, redirectUrl, Request params) reaches the Redirect() call.

      2. If vulnerable, replace with LocalRedirect(), add Url.IsLocalUrl() validation, or use an allowlist.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: Potential open redirect.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this redirect. Do NOT review the rest of the file for other issues.

      1. Confirm whether user input (returnUrl, redirectUrl, Request params) reaches the Redirect() call.

      2. If vulnerable, replace with LocalRedirect(), add Url.IsLocalUrl() validation, or use an allowlist.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: Potential open redirect.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this redirect. Do NOT review the rest of the file for other issues.

      1. Confirm whether user input (returnUrl, redirectUrl, Request params) reaches the Redirect() call.

      2. If vulnerable, replace with validation with startsWith() or URL whitelist checking.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  precision_unsafe_math:
    base: 'CODE QUALITY: float/double used where decimal precision may be required.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this precision issue. Do NOT review the rest of the file for other issues.

      1. Confirm whether this variable participates in financial calculations (price, amount, rate, margin).

      2. If so, change the type from float/double to decimal.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'CODE QUALITY: float/double used where decimal precision may be required.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this precision issue. Do NOT review the rest of the file for other issues.

      1. Confirm whether this variable participates in financial calculations (price, amount, rate, margin).

      2. If so, change the type from float/double to decimal.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'CODE QUALITY: float/double used where decimal precision may be required.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this precision issue. Do NOT review the rest of the file for other issues.

      1. Confirm whether this variable participates in financial calculations (price, amount, rate, margin).

      2. If so, change the type from float/double to decimal.

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  python_call:
    base: 'CROSS-TECHNOLOGY: C# code invokes Python.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this Python interop call. Do NOT review the rest of the file for other issues.

      1. Document the purpose of the Python call (calculation engine, data processing, ML model, etc.).

      2. Assess error handling: what happens if the Python process fails, times out, or returns unexpected output?

      3. Check for input sanitization if C# data is passed to the Python script.

      4. Evaluate whether this is the right integration approach or if a gRPC/REST/message-queue boundary would be safer.

      5. Show any minimal improvements needed -- do not refactor surrounding code.'
    csharp: 'CROSS-TECHNOLOGY: C# code invokes Python.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this Python interop call. Do NOT review the rest of the file for other issues.

      1. Document the purpose of the Python call (calculation engine, data processing, ML model, etc.).

      2. Assess error handling: what happens if the Python process fails, times out, or returns unexpected output?

      3. Check for input sanitization if C# data is passed to the Python script.

      4. Evaluate whether this is the right integration approach or if a gRPC/REST/message-queue boundary would be safer.

      5. Show any minimal improvements needed -- do not refactor surrounding code.'
    java: 'CROSS-TECHNOLOGY: C# code invokes Python.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this Python interop call. Do NOT review the rest of the file for other issues.

      1. Document the purpose of the Python call (calculation engine, data processing, ML model, etc.).

      2. Assess error handling: what happens if the Python process fails, times out, or returns unexpected output?

      3. Check for input sanitization if C# data is passed to the Python script.

      4. Evaluate whether this is the right integration approach or if a gRPC/REST/message-queue boundary would be safer.

      5. Show any minimal improvements needed -- do not refactor surrounding code.'
  sql_injection:
    base: 'SECURITY FINDING: Potential SQL injection via string concatenation/interpolation.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this SQL injection risk. Do NOT review the rest of the file for other issues.

      1. Confirm whether user-controlled input reaches this SQL string.

      2. If vulnerable, rewrite using parameterized queries (@param, SqlParameter, or EF Core interpolated SQL).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: Potential SQL injection via string concatenation/interpolation.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this SQL injection risk. Do NOT review the rest of the file for other issues.

      1. Confirm whether user-controlled input reaches this SQL string.

      2. If vulnerable, rewrite using parameterized queries (@param, SqlParameter, or EF Core interpolated SQL).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: Potential SQL injection via string concatenation/interpolation.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this SQL injection risk. Do NOT review the rest of the file for other issues.

      1. Confirm whether user-controlled input reaches this SQL string.

      2. If vulnerable, rewrite using parameterized queries (PreparedStatement with ? placeholders or JPA criteria queries).

      3. Show the minimal code change needed -- do not refactor surrounding code.'
  sync_over_async:
    base: 'BUG: Sync-over-async anti-pattern detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this sync-over-async call. Do NOT review the rest of the file for other issues.

      1. Identify the pattern: .Result, .Wait(), or .GetAwaiter().GetResult().

      2. Determine if the calling method can be made async (add async/await up the call chain).

      3. If it cannot be async (e.g., constructor, event handler), explain the threading risk and propose a safe alternative.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'BUG: Sync-over-async anti-pattern detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this sync-over-async call. Do NOT review the rest of the file for other issues.

      1. Identify the pattern: .Result, .Wait(), or .GetAwaiter().GetResult().

      2. Determine if the calling method can be made async (add async/await up the call chain).

      3. If it cannot be async (e.g., constructor, event handler), explain the threading risk and propose a safe alternative.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'BUG: Sync-over-async anti-pattern detected.

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this sync-over-async call. Do NOT review the rest of the file for other issues.

      1. Identify the pattern: CompletableFuture.get(), Future.get(), or .join().

      2. Determine if the calling method can be made async (use CompletableFuture composition or make method return Future).

      3. If it cannot be async (e.g., constructor, event handler), explain the threading risk and propose a safe alternative.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
  weak_crypto:
    base: "SECURITY FINDING: Weak cryptographic algorithm in use.\nFile: {file} (line {line})\nContext: {context}\n\nTASK:\
      \ Evaluate ONLY this weak crypto usage. Do NOT review the rest of the file for other issues.\n1. Identify the algorithm\
      \ (MD5, SHA1, DES, TripleDES, RC2).\n2. Determine whether it is used for security (signatures, passwords, tokens, certificates)\n\
      \   or non-security purposes (checksums, cache keys, ETags, content fingerprints, deduplication).\n   MD5/SHA1 for non-security\
      \ hashing is generally acceptable and not a vulnerability.\n3. If security-relevant, replace with SHA-256/SHA-512 (hashing),\
      \ AES-256 (encryption), or PBKDF2/Argon2 (passwords).\n4. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
    csharp: "SECURITY FINDING: Weak cryptographic algorithm in use.\nFile: {file} (line {line})\nContext: {context}\n\nTASK:\
      \ Evaluate ONLY this weak crypto usage. Do NOT review the rest of the file for other issues.\n1. Identify the algorithm\
      \ (MD5, SHA1, DES, TripleDES, RC2).\n2. Determine whether it is used for security (signatures, passwords, tokens, certificates)\n\
      \   or non-security purposes (checksums, cache keys, ETags, content fingerprints, deduplication).\n   MD5/SHA1 for non-security\
      \ hashing is generally acceptable and not a vulnerability.\n3. If security-relevant, replace with SHA-256/SHA-512 (hashing),\
      \ AES-256 (encryption), or PBKDF2/Argon2 (passwords).\n4. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
    java: "SECURITY FINDING: Weak cryptographic algorithm in use.\nFile: {file} (line {line})\nContext: {context}\n\nTASK:\
      \ Evaluate ONLY this weak crypto usage. Do NOT review the rest of the file for other issues.\n1. Identify the algorithm\
      \ (MD5, SHA1, DES).\n2. Determine whether it is used for security (signatures, passwords, tokens, certificates)\n  \
      \ or non-security purposes (checksums, cache keys, ETags, content fingerprints, deduplication).\n   MD5/SHA1 for non-security\
      \ hashing is generally acceptable and not a vulnerability.\n3. If security-relevant, replace with SHA-256/SHA-512 (hashing),\
      \ AES-256 (encryption), or PBKDF2/BCrypt (passwords).\n4. Show the minimal code change needed -- do not refactor surrounding\
      \ code."
  xss:
    base: 'SECURITY FINDING: Potential cross-site scripting (XSS).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this XSS risk. Do NOT review the rest of the file for other issues.

      1. Determine the vector: Html.Raw with user input, Response.Write, or missing [ValidateAntiForgeryToken].

      2. For Html.Raw: encode via HtmlEncoder or use Razor''s default encoding.

      3. For missing anti-forgery: add [ValidateAntiForgeryToken] to the POST action.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
    csharp: 'SECURITY FINDING: Potential cross-site scripting (XSS).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this XSS risk. Do NOT review the rest of the file for other issues.

      1. Determine the vector: Html.Raw with user input, Response.Write, or missing [ValidateAntiForgeryToken].

      2. For Html.Raw: encode via HtmlEncoder or use Razor''s default encoding.

      3. For missing anti-forgery: add [ValidateAntiForgeryToken] to the POST action.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
    java: 'SECURITY FINDING: Potential cross-site scripting (XSS).

      File: {file} (line {line})

      Context: {context}


      TASK: Evaluate ONLY this XSS risk. Do NOT review the rest of the file for other issues.

      1. Determine the vector: PrintWriter.write() without encoding or missing CSRF protection.

      2. For Html.Raw: encode via StringEscapeUtils or use JSTL <c:out>.

      3. For missing anti-forgery: add CSRF token validation to POST endpoints.

      4. Show the minimal code change needed -- do not refactor surrounding code.'
version: 1
